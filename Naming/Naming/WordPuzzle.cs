namespace Naming
{
    public class WordPuzzle
    {
        /*
         * 1. Belirli bir dosyadan rastgele bir kelime seç.
         * 2. Bu kelimenin her harfini *'a dönüştür.
         * 3. Kullanıcının girdiği harfi denetle.
         * 4. Harf varsa; ilgili harfin bulunduğu pozisyondaki *'ı harf ile değiştir.
         * 5. Tahmin etmesini iste.
         * 6. Tekrar et...
         * 
         */

        public string GetRandomWordInFile(string wordsFilePath)
        {
            return string.Empty;
        }

        public string GetPuzzledWord(string realWord)
        {
            return string.Empty;
        }

        public bool IsWordContain(char letter)
        {
            return false;
        }

        public string GetPuzzledWordWithLetterIsFind(string realWord, char letter)
        {
            return string.Empty;
        }

        public void TakeGuest(string guestingWord)
        {
            //FileStream fileStream = new FileStream("", FileMode.CreateNew);

        }
    }
}
